export const zIndex = {
    base: 0,
    dropdown: 1000,
    modal: 2000,
    toast: 3000,
    tooltip: 4000,
};

// Always use these values when setting zIndex. This prevents overlapping and stacking issues.
