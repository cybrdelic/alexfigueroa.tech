export const getProjectPrimaryColors = () => {
    const x1dra: string = 'rgba(26,113,148,100)'

    const extranyx: string = `rgba(84,62,133,100)`

    const cybrnet: string = `rgba(225,44,91,100)`;

    const oversoulDb: string = `rgba(233,140,76,100)`

    const portfolio: string = `rgba(25,113,114,100)`

    return { x1dra, extranyx, cybrnet, oversoulDb, portfolio }
}
